langcode: en
status: true
dependencies:
  module:
    - flag
id: flag_action.session_schedule.unflag
label: 'Remove from my schedule'
type: node
plugin: 'flag_action:session_schedule.unflag'
configuration:
  flag_id: session_schedule
  flag_action: unflag
