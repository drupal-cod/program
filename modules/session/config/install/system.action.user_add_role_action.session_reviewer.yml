langcode: en
status: true
dependencies:
  config:
    - user.role.session_reviewer
  module:
    - user
id: user_add_role_action.session_reviewer
label: 'Add the Session Reviewer role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: session_reviewer
